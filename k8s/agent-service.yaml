apiVersion: v1
kind: Service
metadata:
  name: weather-agent
  labels:
    app: weather-agent
spec:
  type: ClusterIP
  selector:
    app: weather-agent
  ports:
    - name: http
      port: 8000
      targetPort: 8000
