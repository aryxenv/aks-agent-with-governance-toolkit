apiVersion: v1
kind: Service
metadata:
  name: weather-client
  labels:
    app: weather-client
spec:
  type: LoadBalancer
  selector:
    app: weather-client
  ports:
    - name: http
      port: 80
      targetPort: 80
